# REMOVED: Private key was committed to version control and is now compromised.
# ACTION REQUIRED: This private key must be rotated (revoked and reissued).
# The old key remains in git history and should be considered fully exposed.
#
# To provide a TLS private key at runtime, either:
#   1. Mount a new key file at this path (certs/server.key)
#   2. Use services/gencert.sh to generate new development certificates
#   3. Re-run keystore.sh after placing a new server.key and server.crt here
#
# Do NOT commit real private keys to version control.
